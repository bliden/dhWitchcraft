import { DriftzoomDirective } from './driftzoom.directive';

describe('DriftzoomDirective', () => {
  it('should create an instance', () => {
    const directive = new DriftzoomDirective();
    expect(directive).toBeTruthy();
  });
});
