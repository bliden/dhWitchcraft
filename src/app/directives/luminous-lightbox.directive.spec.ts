import { LuminousLightboxDirective } from './luminous-lightbox.directive';

describe('LuminousLightboxDirective', () => {
  it('should create an instance', () => {
    const directive = new LuminousLightboxDirective();
    expect(directive).toBeTruthy();
  });
});
